{
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "poetry run python src/app.py",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE"
  }
}
